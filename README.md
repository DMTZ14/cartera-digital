# Cartera Digital

Aplicación de consola en Python para llevar control de ingresos, gastos y presupuestos personales usando un archivo de Excel como base de datos.

## Requisitos

- Python 3.10 o superior  
- Librería `openpyxl`

Instala dependencias con:

```bash
pip install openpyxl
